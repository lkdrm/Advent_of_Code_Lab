# Puzzle inputs

## Demo inputs

`inputs/demo` contains small custom inputs created for this repository.

They are committed so anyone can clone the project and run the CLI immediately.

## Personal inputs

`inputs/local` is ignored by Git.

Add your own Advent of Code input files there:

```text
inputs/local/{year}/day{day:00}.txt